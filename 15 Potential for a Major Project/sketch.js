// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"

const g = 9.81;
let scale;

function findDisplacement(v0,a) {
  return v0 * deltaTime + (1/2) * a * deltaTime ** 2;
}

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}

class Ball {
  constructor(vX, vY) {

  }
}